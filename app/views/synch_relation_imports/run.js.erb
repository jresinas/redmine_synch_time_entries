$('#import-progress').progressbar({value: <%= @current.to_i %>});
$('#progress-label').text("<%= @current.to_i %> / <%= @import.total_items.to_i %>");

<% if @import.finished? %>
window.location.href='<%= synch_relation_import_path(@import) %>';
<% else %>
$.ajax({
  url: '<%= synch_relation_import_run_path(@import, :format => 'js') %>',
  type: 'post'
});
<% end %>
